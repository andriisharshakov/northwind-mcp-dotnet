meta {
  name: Get All Products
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/products
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

query {
  ~search: chai
  ~limit: 20
}
